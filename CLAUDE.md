# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a minimal project currently in early development stages. The project name "fonderia:dev" suggests it may be a development/branch version of a larger project.

## MCP Server Configuration

The project has an MCP server configured for shadcn UI components:
- Server is accessible via `npx shadcn@latest mcp`
- shadcn MCP server is enabled in local Claude settings

## Development Commands

No specific build, test, or development commands are currently configured in package.json. As the project grows, update this section with relevant npm scripts.

## Dependencies

- **shadcn** (^3.4.2): UI component library - available as dev dependency
